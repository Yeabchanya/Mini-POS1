spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/MiniPOS1
    username: MiniPOS
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect