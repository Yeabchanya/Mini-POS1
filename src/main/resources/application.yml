spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/MiniPOS1
    username: MiniPOS
    password: MiniPOS123
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher